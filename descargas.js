document.getElementById('btnDescargar').addEventListener('click', function() {
    // URL del archivo PDF
    var pdfUrl = './mru.pdf';
  
    // Crear un enlace temporal
    var link = document.createElement('a');
    link.href = pdfUrl;
    link.download = 'Formulario.pdf';
    link.target = '_blank';
  
    // Agregar el enlace temporal al documento
    document.body.appendChild(link);
  
    // Simular un clic en el enlace para iniciar la descarga
    link.click();
  
    // Eliminar el enlace temporal del documento
    document.body.removeChild(link);
  });

  document.getElementById('btnDescargar2').addEventListener('click', function() {
    // URL del archivo PDF
    var pdfUrl = './mru.pdf';
  
    // Crear un enlace temporal
    var link = document.createElement('a');
    link.href = pdfUrl;
    link.download = 'Formulario.pdf';
    link.target = '_blank';
  
    // Agregar el enlace temporal al documento
    document.body.appendChild(link);
  
    // Simular un clic en el enlace para iniciar la descarga
    link.click();
  
    // Eliminar el enlace temporal del documento
    document.body.removeChild(link);
  });

  document.getElementById('btnDescargar3').addEventListener('click', function() {
    // URL del archivo PDF
    var pdfUrl = './mru.pdf';
  
    // Crear un enlace temporal
    var link = document.createElement('a');
    link.href = pdfUrl;
    link.download = 'Formulario.pdf';
    link.target = '_blank';
  
    // Agregar el enlace temporal al documento
    document.body.appendChild(link);
  
    // Simular un clic en el enlace para iniciar la descarga
    link.click();
  
    // Eliminar el enlace temporal del documento
    document.body.removeChild(link);
  });

  document.getElementById('btnDescargar4').addEventListener('click', function() {
    // URL del archivo PDF
    var pdfUrl = './mru.pdf';
  
    // Crear un enlace temporal
    var link = document.createElement('a');
    link.href = pdfUrl;
    link.download = 'Formulario.pdf';
    link.target = '_blank';
  
    // Agregar el enlace temporal al documento
    document.body.appendChild(link);
  
    // Simular un clic en el enlace para iniciar la descarga
    link.click();
  
    // Eliminar el enlace temporal del documento
    document.body.removeChild(link);
  });